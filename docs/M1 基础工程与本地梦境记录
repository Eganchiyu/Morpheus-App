# M1 基础工程与本地梦境记录

## 1. 里程碑信息

| 项目 | 内容 |
|---|---|
| 里程碑 | M1 |
| 对应版本 | Version 0.1 |
| 建议周期 | 7 至 10 个工作日 |
| 当前状态 | 计划中 |
| 核心交付 | 可在 iOS 与 Android 上运行的本地梦境记录应用 |

## 2. 目标

M1 的目标是建立稳定、可扩展的 Flutter 工程基础，并完成第一条可独立验证的核心业务链路：

```text
启动应用 -> 输入当天梦境 -> 自动保存 -> 关闭应用 -> 再次启动后继续查看和编辑
```

本里程碑完成后，项目应当具备可持续开发的基础工程、明确的数据模型、可靠的本地持久化能力，以及一个可实际使用的梦境编辑页面。

## 3. 范围

### 3.1 包含内容

- 初始化 Flutter iOS 与 Android 工程。
- 建立基础目录结构、应用主题与路由。
- 配置 Riverpod 状态管理。
- 配置 Drift 与 SQLite 本地数据库。
- 定义 `DreamEntry` 领域模型及数据库表。
- 实现当天梦境的创建、读取、更新和删除。
- 实现多行文本编辑与自动保存。
- 展示保存状态和基础错误提示。
- 配置静态检查、单元测试与 GitHub Actions。
- 完成 iOS 与 Android 的基础运行验证。
- 更新 README 中的安装和运行说明。

### 3.2 不包含内容

- 日历和历史记录浏览。
- 本地定时通知。
- HealthKit、Health Connect 或智能手表数据。
- 用户账户、云同步和后端服务。
- AI 梦境画报、周期报告和内容推荐。
- 标签、心情、图片、富文本和复杂编辑样式。
- 数据导出、分享和付费功能。

上述功能不得阻塞 M1 的交付。如开发过程中出现相关需求，应记录到后续里程碑，不在 M1 内扩展范围。

## 4. 技术基线

| 层级 | 选型 |
|---|---|
| 客户端 | Flutter 与 Dart |
| 状态管理 | Riverpod |
| 路由 | go_router |
| 本地数据库 | Drift 与 SQLite |
| 代码检查 | flutter_lints 与 `flutter analyze` |
| 测试 | flutter_test 与 Drift 数据层测试 |
| 持续集成 | GitHub Actions |

项目包名、最低 iOS 版本和最低 Android SDK 版本应在工程初始化前确定，并记录在开发日志中。M1 不引入尚未使用的第三方依赖。

## 5. 工程结构

M1 建议采用按功能组织的轻量目录结构：

```text
lib/
├── main.dart
├── app/
│   ├── app.dart
│   ├── router.dart
│   └── theme.dart
├── core/
│   └── db/
│       ├── app_database.dart
│       └── tables/
└── features/
    └── dream_editor/
        ├── data/
        │   └── dream_repository.dart
        ├── domain/
        │   └── dream_entry.dart
        └── presentation/
            ├── dream_editor_controller.dart
            └── dream_editor_page.dart

test/
├── core/db/
└── features/dream_editor/
```

目录结构应服务于当前功能，不在 M1 阶段预先创建 AI、报告、推荐或后端相关的空模块。

## 6. 数据模型

M1 使用单一核心实体 `DreamEntry`：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | 整数或 UUID | 记录唯一标识 |
| `dreamDate` | 本地日期 | 梦境所属日期，不包含时间 |
| `content` | 字符串 | 梦境正文 |
| `createdAt` | UTC 时间 | 首次创建时间 |
| `updatedAt` | UTC 时间 | 最近更新时间 |

M1 业务规则：

- 每个本地日期最多存在一条梦境记录。
- 保存相同日期时更新原记录，不重复创建。
- 纯空白内容不创建新记录。
- 清空已有内容时，由产品逻辑明确执行删除或保留空记录；M1 默认执行删除。
- 展示日期使用设备本地时区，创建和更新时间使用 UTC 保存。
- 数据库访问必须通过 Repository 层完成，页面不得直接操作 Drift 表。

## 7. 功能要求

### 7.1 应用启动

- 应用能够在受支持的 iOS 与 Android 环境中启动。
- 启动后进入当天梦境编辑页面。
- 数据库初始化失败时应显示可理解的错误信息，不得无限加载或直接崩溃。

### 7.2 梦境编辑

- 页面显示当前日期。
- 页面提供支持多行输入的正文编辑区域。
- 如果当天已有记录，进入页面后自动加载正文。
- 输入过程中不得出现明显卡顿。
- M1 不提供字体、颜色、背景和富文本设置。

### 7.3 自动保存

- 用户停止输入约 800 毫秒后触发自动保存。
- 应用进入后台或页面被关闭前，应提交最后一次待保存内容。
- 连续输入期间应避免每个字符都写入数据库。
- 页面应显示以下状态：`未保存`、`保存中`、`已保存`、`保存失败`。
- 保存失败后保留编辑器内的文本，并允许再次尝试保存。

### 7.4 本地持久化

- 关闭并重新启动应用后，已保存内容必须仍然存在。
- 更新已有记录不得产生重复数据。
- 删除记录后，再次启动应用不得重新出现已删除内容。

## 8. 工作项

### M1-01 工程与仓库基线

- 确定包名和最低系统版本。
- 初始化 Flutter 工程。
- 配置 `.gitignore`、lint 和基础依赖。
- 移除 Flutter 示例计数器代码。
- 确认 iOS 与 Android 调试构建可运行。

完成条件：全新克隆仓库后，可以按照 README 在两个平台完成依赖安装和应用启动。

### M1-02 应用外壳

- 创建应用入口、主题和路由。
- 建立当天梦境编辑页的空页面。
- 处理基础加载、空状态和错误状态。

完成条件：应用启动后稳定进入编辑页面，导航结构不存在明显错误。

### M1-03 数据库与领域模型

- 定义 `DreamEntry`。
- 创建 Drift 表和数据库类。
- 建立初始数据库版本。
- 实现 Repository 接口与具体实现。

Repository 至少提供：

```dart
Future<DreamEntry?> getDreamByDate(DateTime date);

Future<void> saveDream({
  required DateTime date,
  required String content,
});

Future<void> deleteDream(DateTime date);
```

完成条件：数据层测试覆盖创建、读取、更新、删除和同日去重。

### M1-04 编辑器与状态管理

- 创建多行文本编辑器。
- 通过 Riverpod 管理加载、编辑和保存状态。
- 加载当天已有记录。
- 实现自动保存与保存状态展示。
- 处理页面退出和应用进入后台时的待保存内容。

完成条件：用户能够完成当天梦境的记录、修改和持久化。

### M1-05 测试与持续集成

- 为日期归一化和空内容规则编写单元测试。
- 为 Repository 编写数据库测试。
- 为编辑器的加载与保存状态编写 Widget 测试。
- 配置 GitHub Actions 执行格式检查、静态分析和测试。

完成条件：Pull Request 或推送触发 CI，全部检查通过后方可合并到 `main`。

### M1-06 双平台验证与文档

- 在 iOS 模拟器或真机完成冒烟测试。
- 在 Android 模拟器或真机完成冒烟测试。
- 使用全新安装验证数据库初始化。
- 使用升级安装验证数据不会无故丢失。
- 更新 README 的真实仓库地址、环境要求和启动命令。
- 在开发日志中记录 M1 的完成情况和遗留问题。

完成条件：两个平台均完成完整的记录、保存、重启和编辑流程。

## 9. 建议进度

| 工作日 | 计划 |
|---|---|
| 第 1 天 | 技术基线确认、Flutter 工程初始化、双平台启动 |
| 第 2 天 | 应用入口、主题、路由和编辑页外壳 |
| 第 3 至 4 天 | Drift 数据库、领域模型和 Repository |
| 第 5 至 6 天 | 编辑器、Riverpod 状态管理和自动保存 |
| 第 7 天 | 单元测试、Widget 测试和 CI |
| 第 8 天 | iOS 与 Android 冒烟测试 |
| 第 9 至 10 天 | 缺陷修复、文档更新和交付缓冲 |

进度允许提前，但不得以删除关键测试或跳过双平台验证作为提前完成的手段。

## 10. 测试场景

M1 至少验证以下场景：

1. 首次安装后应用正常启动。
2. 当天没有记录时显示空编辑器。
3. 输入多行文字后显示保存状态变化。
4. 应用完全关闭并重新启动后内容仍然存在。
5. 修改已有内容后不会产生第二条同日记录。
6. 快速连续输入时不会出现明显卡顿或保存顺序错误。
7. 输入纯空白内容时不会创建记录。
8. 清空已有记录后按既定规则删除数据。
9. 应用输入过程中进入后台，返回后内容不丢失。
10. 数据库异常时页面显示失败状态并保留尚未保存的文本。

## 11. 风险与控制措施

| 风险 | 控制措施 |
|---|---|
| 自动保存发生竞态，旧内容覆盖新内容 | 对保存请求进行串行化，并只确认最新版本 |
| 日期及时区处理不一致 | 使用统一的本地日期归一化函数，并单独测试 |
| 页面与数据库耦合过高 | 通过 Repository 隔离 Drift 实现 |
| 初期架构过度复杂 | 只创建 M1 实际使用的模块和抽象 |
| 第三方依赖过多 | 仅引入当前工作项必需的依赖 |
| 双平台行为不一致 | 在 M1 结束前执行相同的双平台冒烟测试 |

## 12. 完成定义

满足以下全部条件后，M1 方可标记为完成：

- 全新克隆仓库后可按照 README 成功运行项目。
- iOS 与 Android 调试构建均可启动。
- 用户能够创建、查看、修改和删除当天梦境。
- 自动保存行为稳定，重启应用后数据仍然存在。
- 相同日期不会产生重复记录。
- 页面能够展示保存进度和失败状态。
- `dart format` 检查通过。
- `flutter analyze` 无错误。
- `flutter test` 全部通过。
- GitHub Actions 全部通过。
- 仓库中不存在密钥、Token、证书或真实用户数据。
- README 和开发日志已更新。
- 已知问题已记录，并明确归入后续里程碑。

## 13. 后续里程碑接口

M1 应为后续功能保留清晰扩展点，但不提前实现：

- M2：历史记录列表与日历回顾。
- M3：用户设定时间的本地通知。
- M4：HealthKit 与 Health Connect 技术验证及醒来提醒。
- M5：测试、隐私完善与首轮内测发布。

M1 结束时最重要的结果不是页面数量，而是一条经过测试、数据可靠且可在两个平台运行的核心记录链路。
